# Stale / Idle File Audit (read-only scan — nothing deleted)

Scanned: 2026-08-25. Scope: build artifacts, logs, caches, generated output, large
untracked files. **No files were removed.** Sizes via `du`; tracked status via `git ls-files`.

## Safe to delete outright (regenerable, not tracked, no user data)

| Path | Size | Why safe to remove |
|---|---|---|
| `ui/dist/` | 425 KB | Vite production build artifact; regenerated by `npm run build` (full UI builds are not tracked/published) |
| `__pycache__/` dirs (17, outside `.venv`/`node_modules`) | ~tens of KB | Python bytecode cache; regenerated on import; already gitignored |
| `.pytest_cache/` | 27 KB | pytest cache; regenerated on next run; gitignored |
| `.benchmarks/` | 0 | Empty pytest-benchmark slot; regenerated by benchmark runs |
| `candidate_intelligence.db` (repo root) | 0 bytes | Stale empty SQLite file; the active DB is `storage/cip_main.db` (456K, WAL) |
| `*.tmp` / `*.bak` / `*.orig` (repo scan) | none found | (No action needed — scanned, nothing present) |
| `*.log` files (repo scan) | none found | (No action needed — scanned, nothing present; `*.log` is gitignored) |

## Safe to remove on confirmation (generated / regenerable, but tracked in git)

| Path | Size | Reason |
|---|---|---|
| `graphify-out/` | 15 MB (362 tracked files) | Graphify analysis output (dated `2026-08-20` … `2026-08-25` snapshots, `graph.json` 1.6M, `graph.html` 1.2M, `cache/last_query_stamp` which churns on every run — it shows up as an unstaged modification in plain `git status`). Regenerable by re-running graphify. Recommend: `git rm -r graphify-out` + add `graphify-out/` to `.gitignore` |
| `ingestion_reports/` | 72 KB (3 tracked files) | Generated bulk-ingest output (`bulk_ingest_checkpoint.json`, `bulk_ingest_report.json`, `bulk_ingest_summary.md`). Report churn pollutes history. Recommend: keep the code that writes them; stop committing them (untrack + gitignore) |
| `docs/chats/*` | 32 KB (1 tracked file) | Chat transcripts of previous AI sessions; historical reference only, no code. Keep if you want the history; otherwise untrack. |

## Large dependency dirs — NOT removal candidates

| Path | Size | Why keep |
|---|---|---|
| `.venv/` | 709 MB | Active project virtualenv (Python 3.14). Removing forces a full `uv sync` (minutes, network); no repo correctness issue if removed, but not recommended |
| `ui/node_modules/` | 188 MB | npm dependency tree; removing requires `npm ci`. Gitignored |

## Runtime/user data — keep (do not delete)

- `storage/cip_main.db` (+ `-shm`/`-wal`): active SQLite DB (WAL), gitignored.
- `storage/documents/` (2.5M) CAS blobs: immutable SHA-256 store, gitignored.
- `storage/lancedb/` (1.6M): embedded vector store, gitignored.
- `test-resumes/` (2.5M): test fixtures, gitignored.
- `backups/`: source module (`backup_manager.py`), not a backup dump — keep.
- `.backpass/` (896K) / `.freebuff/` (257K): agent tool state/caches — leave untouched.

## Notes

- No `.env`, keys, certs, or credentials are tracked by git (`.env` is gitignored).
- `ui/dist/` and `ui/node_modules/` are ignored by `ui/.gitignore`; `graphify-out/` and
  `ingestion_reports/` are **not** gitignored — that's why they keep showing up in `git status`.

**Suggested next step (user action):** after confirming this list, run:

```bash
rm -rf ui/dist __pycache__ .pytest_cache .benchmarks candidate_intelligence.db
git rm -r --cached graphify-out ingestion_reports && echo "graphify-out/" >> .gitignore
```